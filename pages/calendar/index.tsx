import Layout from "@components/layout";
import { NextPage } from "next";

const Calendar: NextPage = () => {
  return (
    <Layout hasHeader hasTabBar>
      <div>Calender</div>
    </Layout>
  );
};

export default Calendar;
